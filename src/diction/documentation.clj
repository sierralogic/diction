(ns diction.documentation
  (:require [diction.core :as diction]))

(defn entity-report
  ([] (entity-report nil))
  ([entity-ids]

    ))

(defn metadata-report
  ([] (metadata-report nil))
  ([element-ids]
    ))
